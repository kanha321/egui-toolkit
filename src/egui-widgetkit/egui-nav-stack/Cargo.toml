[package]
name = "egui-nav-stack"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "App-owned back-stack screen navigation system for egui, modeled on Android Navigation 3"

[dependencies]
# 0.27 — pinned workspace-wide; see CODING_RULES §6
egui = { workspace = true }
egui-spring = { path = "../egui-spring", optional = true }

[dev-dependencies]
eframe = { workspace = true }

[features]
default = []
animated-transitions = ["dep:egui-spring"]
