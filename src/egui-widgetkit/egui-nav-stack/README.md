# egui-nav-stack

App-owned back-stack screen navigation system for egui, modeled on Android Navigation 3.

## Philosophy
The back stack is a plain list the consuming application owns and passes in — not hidden state inside a controller object.
