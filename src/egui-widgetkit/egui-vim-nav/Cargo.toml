[package]
name = "egui-vim-nav"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Vim-style (HJKL) keyboard navigation graph for egui"

[dependencies]
# 0.27 — pinned workspace-wide; see CODING_RULES §6
egui = { workspace = true }

[dev-dependencies]
eframe = { workspace = true }
